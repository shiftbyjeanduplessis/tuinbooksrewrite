import { supabase } from '../../lib/supabase.js';
export interface ManagedBusiness {id:string;name:string;phone:string;email:string;onboardingComplete:boolean;createdAt:string;support:{status:string;expiresAt:string|null;operationalRead:boolean;operationalEdit:boolean;financialRead:boolean;financialEdit:boolean}|null;health:{clients:number;locations:number;teams:number;futureVisits:number;openInvoices:number};}
export async function listManagedBusinesses(search=''):Promise<ManagedBusiness[]>{const{data,error}=await supabase.rpc('tuinbooks_v2_management_list_businesses',{p_search:search});if(error)throw explain(error);return(data??[]) as ManagedBusiness[];}
export async function setSupportGrant(businessId:string,input:{status:'active'|'revoked';expiresAt:string|null;operationalRead:boolean;operationalEdit:boolean;financialRead:boolean;financialEdit:boolean}){const{error}=await supabase.rpc('tuinbooks_v2_management_set_support_grant',{p_business_id:businessId,p_status:input.status,p_expires_at:input.expiresAt,p_operational_read:input.operationalRead,p_operational_edit:input.operationalEdit,p_financial_read:input.financialRead,p_financial_edit:input.financialEdit});if(error)throw explain(error);}
export async function currentStaff():Promise<any>{const{data,error}=await supabase.rpc('tuinbooks_v2_management_current_staff',{});if(error)throw explain(error);return data;}
function explain(e:any):Error{if(/platform staff|not authorised|permission/i.test(String(e?.message??e)))return new Error('This account is not authorised for TuinBooks Management.');if(/does not exist|schema cache|PGRST20[25]|42883/i.test(String(e?.message??e)))return new Error('The TuinBooks v2 Management migration has not been installed in this database.');return e instanceof Error?e:new Error(String(e?.message??e));}
