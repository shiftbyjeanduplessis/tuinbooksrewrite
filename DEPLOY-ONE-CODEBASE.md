# R14 deployment

Supabase first: run `supabase/APPLY-R14-VISIT-DETAIL.sql`.

Then replace the contents of the local `tuinbooksrewrite` repository with the contents of this release folder, commit and push.

Suggested commit summary: `Restore visit work details`

Render proof line:
`TUINBOOKS UI-RESTORED RELEASE R14: visit work detail restored + simplified administrative actions published to /management/ + /app/.`
