'use strict';
const assert=require('assert');
const fs=require('fs');
const path=require('path');
const root=path.join(__dirname,'..');
const index=fs.readFileSync(path.join(root,'index.html'),'utf8');
const app=fs.readFileSync(path.join(root,'app.js'),'utf8');
const css=fs.readFileSync(path.join(root,'schedule-foundation.css'),'utf8');
const sw=fs.readFileSync(path.join(root,'service-worker.js'),'utf8');
assert(index.includes('56.0.5-schedule-card-format1'),'HTML assets must use the current cumulative cache token');
assert(sw.includes('tuinbooks-shell-v56-0-5-schedule-card-format1'),'Service worker cache must be bumped');
assert(index.includes('<h1>Marketing — Coming soon</h1>'),'Marketing page must be labelled Coming soon');
assert(index.includes('<h1>Mileage — Coming soon</h1>'),'Mileage page must be labelled Coming soon');
assert(index.includes('Opportunities &amp; Quotes'),'Office navigation must expose Opportunities & Quotes');
assert(index.indexOf('id="weekReviewPanel"')<index.indexOf('id="weeklyScheduleBoard"'),'Week review must sit above the calendar');
assert(app.includes("queueSource:'could-not-fit'"),'Scheduling queue must support work the generator could not fit');
assert(app.includes("queueSource:source"),'Scheduling queue must include missed/catch-up work');
assert(app.includes('Work to schedule'),'Scheduling queue must expose the manual Work to schedule workflow');
assert(app.includes('reconcileGeneratedOverflowV5544'),'Smart scheduling must move genuine overflow into the queue');
assert(app.includes('schedule-queue-move-handle'),'Queue window must have a drag handle');
assert(app.includes("if(activeView==='marketing'||activeView==='mileage')return"),'Coming-soon features must not run live workflows');
assert(app.includes("if(/Mark ready/i.test(button.textContent||''))button.remove()"),'Billing must remove the manual Ready step');
assert(css.includes('.schedule-top-status-v5544'),'Compact week and queue status must be styled');
assert(css.includes('cursor:move'),'Scheduling queue must be movable');
console.log('v55.4.4 trial workflow tests passed');
