'use strict';
const assert=require('assert');
const fs=require('fs');
const path=require('path');
const root=path.join(__dirname,'..');
const index=fs.readFileSync(path.join(root,'index.html'),'utf8');
const mobile=fs.readFileSync(path.join(root,'mobile.html'),'utf8');
const app=fs.readFileSync(path.join(root,'app.js'),'utf8');
const css=fs.readFileSync(path.join(root,'schedule-foundation.css'),'utf8');
const sw=fs.readFileSync(path.join(root,'service-worker.js'),'utf8');
assert(index.includes('56.0.5-schedule-card-format1'),'Desktop assets must use the v55.4.7 cache token');
assert(mobile.includes('56.0.5-schedule-card-format1'),'Mobile assets must use the v55.4.7 cache token');
assert(sw.includes('tuinbooks-shell-v56-0-5-schedule-card-format1'),'Service worker cache must be v55.4.7');
assert(index.includes('Recurring visit to-do list'),'Client form must define the recurring mobile to-do list');
assert(app.includes('jobChecklistTasksV5547'),'Mobile checklist must resolve client or quote-specific tasks');
assert(app.includes('quoteChecklistTasksV5547'),'Accepted quote lines must become once-off job tasks');
assert(app.includes("workKind:'once-off'"),'Accepted quotes must create separate once-off schedule jobs');
assert(app.includes('Accepted quote scheduled as separate once-off job'),'Accepted quote jobs must retain a separate audit source');
assert(app.includes("markerA==='R'?-1:1"),'Routine work must be ordered before once-off work at the same site');
assert(app.includes('mobile-work-marker-v5547'),'Mobile jobs must display R/O markers');
assert(app.includes('Create quote'),'Opportunity review must have one clear quote action');
assert(!index.includes('Only start a quote after marking the work as an easy win.'),'Easy-win instruction must be removed');
assert(css.includes('.opportunity-simple-v5547'),'Simple opportunity review styling must exist');
assert(css.includes('.mobile-client-task-list-v5547'),'Client-specific mobile task list styling must exist');
console.log('v55.4.7 client tasks and quote jobs tests passed');
