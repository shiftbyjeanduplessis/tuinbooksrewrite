TUINBOOKS UI-RESTORED RELEASE R6 — WORKSPACE DISPLAY REPAIR

1. Run supabase/APPLY-R6-NON-SCHEDULE-PAGE-READ-BRIDGE.sql in the existing TuinBooks Supabase SQL Editor.
2. It changes authorization/read functions only; it does not modify client data.
3. Copy this complete release over the tuinbooksrewrite repo.
4. Commit and push.
5. Render build proof line must be:
   TUINBOOKS UI-RESTORED RELEASE R6: Management-session workspace reads + Schedule parity build published to /management/ + /app/.
6. Re-open the same account from Management and verify Clients, Work, Quotes, Billing, Business and Settings all render data.
7. Do not run the optional legacy recurrence migration.
